# LLM Calls

CrewAI calls are configured by `src/config/task/tasks.yaml` and executed through the crew classes in `src/reagent/`.

Add one file per call family when changing prompts or agent/tool bindings.
