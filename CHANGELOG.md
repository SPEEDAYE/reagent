# Changelog

## 2026-06-02

- Restructured the repository toward the `academic_skills` academic-code layout.
- Added a root `main.py` orchestration entry and standardized scripts under `script/`.
- Moved runtime examples into `dataset/`, execution artifacts under `experiment/`, and implementation packages under `src/`.
